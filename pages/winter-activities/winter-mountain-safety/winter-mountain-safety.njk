---
layout: base.njk
pagename: winter-mountain-safety
permalink: "build/winter-activities/winter-mountain-safety/index.html"
permalinkBypassOutputDir: true
nickname: winter-mountain-safety
---
{% from "atoms/headings.njk" import h1, h2, h3, h4, h5, h6 %}
{% from 'molecules/card.njk' import verticalCard %}
{% from 'molecules/newsletter.njk' import newsletter %}
{% from 'organisms/global-image.njk' import global_image %}
{% from 'molecules/group-venues.njk' import groupVenues %}
{% from 'molecules/bridge.njk' import bridge %}
{% from 'atoms/accordion.njk' import accordion2 %}
{% from 'macros.njk' import top_banner_group_venues %}

{# Hero Image Section #}
{% if pageCMS['group-venues'].group_venues and pageCMS['top-banner'].global_image %}
    {% set winter_group_venues = pageCMS['group-venues'].group_venues  %}
    {{ top_banner_group_venues(pageCMS['top-banner'].global_image, winter_group_venues) }}
{% endif %}

{% if  pageCMS['contact-info'] and  pageCMS['contact-info'].simple_article %}
<section class="segment-outer">
    <section class="global-contents global-px text-lg-2 font-ivar">
        {{pageCMS['contact-info'].simple_article.description | safe}}
    </section>
</section>
{% endif %} 

{# Accordion #}
{% if  pageCMS['accordion'] %}
{% set accordions = pageCMS['accordion'].accordion %}
<section class="segment-outer my-12">
    <section class="winter-safety__accordion accordion-wrapper global-px flex flex-col gap-2">
        {% for item in accordions %}
            {{ accordion2( item = item ) }}
        {% endfor %}             
    </section>
</section>
{% endif %} 

{# Card Title #}
{% if pageCMS['card-title'] %}
    {% set card_title= pageCMS['card-title'].simple_article %}
    {{ h2(copy = card_title.title, classes = ['winter-card__header', 'text-center', 'font-ivar', 'font-medium', 'text--color__sn-6']) }}
{% endif %}

{# Vertical Card Section #}
{% if  pageCMS['vertical-cards'] %}
{% set vertical_card_list= pageCMS['vertical-cards'].vertical_card_list %}
<section class="section-outer">
    <section class="section-inner winter__card">
        {% for item in vertical_card_list %}
            {{ verticalCard(item = item.vertical_card, card_container = ['card-container', 'mb-8']) }}
        {% endfor %}
    </section>
</section>
{% endif %}

{{newsletter()}}